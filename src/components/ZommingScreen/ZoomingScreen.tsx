import { Grid } from "@mui/material";
import styles from "./zooming-screen.module.css";

export const ZoomingScreen = () => {

    return (
        <Grid className={styles.whiteScreen}></Grid>
    );
};

